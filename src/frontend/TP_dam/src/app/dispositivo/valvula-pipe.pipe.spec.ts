import { ValvulaPipePipe } from './valvula-pipe.pipe';

describe('ValvulaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ValvulaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
