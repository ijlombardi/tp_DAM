import { ColorMenuDirective, } from './color-menu.directive';

describe('ColorMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorMenuDirective();
    expect(directive).toBeTruthy();
  });
});
